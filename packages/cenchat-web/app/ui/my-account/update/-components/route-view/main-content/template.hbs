{{import ProfileForm from "./profile-form"}}

<div class="side-sheet__content" data-test-main-content="host">
  <div class="wrapper">
    <ProfileForm
      @user={{@user}}
      @onProfileUpdateEvent={{fn @onProfileUpdateEvent}}
      @onProfileFormSubmit={{fn @onProfileFormSubmit}}
    />
  </div>

  {{yield}}
</div>
