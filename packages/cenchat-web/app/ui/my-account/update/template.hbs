{{import RouteView from "./-components/route-view"}}

<RouteView
  @user={{this.model}}
  @hasPendingProfileChanges={{this.hasPendingProfileChanges}}
  @onProfileUpdateEvent={{fn this.handleProfileUpdateEvent}}
  @onProfileFormSubmit={{fn this.handleProfileFormSubmit}}
>
  {{outlet}}
</RouteView>
