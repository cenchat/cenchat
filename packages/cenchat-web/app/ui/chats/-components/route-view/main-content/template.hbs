{{import ChatList from "./chat-list"}}

<main class="layout__content" data-test-main-content="host">
  {{#if @chats}}
    <div class="layout__master-pane wrapper wrapper--list">
      <ChatList @chats={{@chats}} />
    </div>
  {{else}}
    <div class="layout__master-pane wrapper">
      <EmptyState
        data-test-main-content="empty-state"
        @title="Nothing in Chats"
        @subtitle="Your chats will show up here"
      />
    </div>
  {{/if}}

  {{yield}}
</main>
