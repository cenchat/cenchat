{{import RouteView from "./-components/route-view"}}

<RouteView
  @site={{this.model}}
  @hasPendingSiteChanges={{this.hasPendingSiteChanges}}
  @onSiteUpdateEvent={{fn this.handleSiteUpdateEvent}}
  @onSiteFormSubmit={{fn this.handleSiteFormSubmit}}
>
  {{outlet}}
</RouteView>
