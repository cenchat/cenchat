{{import MemberListItem from "./member-list-item"}}

<ul class="list" data-test-member-list="host">
  {{#each @members as |member|}}
    <MemberListItem @member={{member}} @type={{@type}} @onRoleChange={{fn @onRoleChange member}} />
  {{/each}}
</ul>
